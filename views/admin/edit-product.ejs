<%- include('../partials/admin/header.ejs') %>

<div class="wrapper">
    <section class="content-main">
        <div class="row">
            <div class="col-10">
                <div class="dashboard-header">
                    <h2 class="content-title">Edit Product</h2>
                </div>
            </div>
            <div class="col-lg-10">
                <div class="card mb-4">
                    <div class="card-body">
                        <form method="post" action="/admin/editProduct/<%= product._id %>" enctype="multipart/form-data"
                            onsubmit="return validateForm()">
                            <div class="mb-4">
                                <label for="product_name" class="form-label">Product Name</label>
                                <input type="text" placeholder="Type here" name="productName" class="form-control border"
                                    id="product_name" value="<%= product.productName %>" required>
                                <div id="productName-error" class="error-message"></div>
                            </div>

                            <div class="col-lg-4 mb-4">
                                <label class="form-label">Brand</label>
                                <select class="form-select border" name="brand">
                                    <option value="" disabled>Select the Brand</option>
                                    <% brand.forEach(brand => { %>
                                        <option value="<%= brand.brandName %>" <%= product.brand === brand.brandName ? 'selected' : '' %>>
                                            <%= brand.brandName %>
                                        </option>
                                    <% }); %>
                                </select>
                                <div id="brand-error" class="error-message"></div>
                            </div>

                            <% for (let i = 1; i <= 4; i++) { %>
                                <div class="mb-4">
                                    <label for="specification_<%= i %>" class="form-label">Specification <%= i %></label>
                                    <input type="text" placeholder="Type here" name="specification<%= i %>"
                                        class="form-control border" id="specification_<%= i %>" 
                                        value="<%= product['specification' + i] %>" required>
                                    <div id="specification<%= i %>-error" class="error-message"></div>
                                </div>
                            <% } %>

                            <div class="mb-4">
                                <label class="form-label">Full Description</label>
                                <textarea placeholder="Type here" id="descriptionid" name="description"
                                    class="form-control border" rows="4"><%= product.description %></textarea>
                                <div id="description-error" class="error-message"></div>
                            </div>

                            <div class="row">
                                <div class="col-lg-4 mb-4">
                                    <label class="form-label">Price</label>
                                    <input placeholder="₹" name="price" type="text" class="form-control border"
                                        value="<%= product.price %>">
                                    <div id="price-error" class="error-message"></div>
                                </div>

                                <div class="col-lg-4 mb-4">
                                    <label class="form-label">Stock Quantity</label>
                                    <input name="quantity" type="text" class="form-control border"
                                        value="<%= product.quantity %>">
                                    <div id="quantity-error" class="error-message"></div>
                                </div>
                            </div>

                            <div class="row">
                                <div class="col-lg-4 mb-4">
                                    <label class="form-label">Color</label>
                                    <input name="color" type="text" class="form-control border"
                                        value="<%= product.color %>">
                                    <div id="color-error" class="error-message"></div>
                                </div>
                            </div>

                            <div class="card mb-4">
                                <div class="card-body">
                                    <div class="row gx-2">
                                        <div class="col-sm-6 mb-3">
                                            <label class="form-label">Category</label>
                                            <select class="form-select border" style="width: 100%;" name="category">
                                                <option value="" disabled>Select a category</option>
                                                <% cat.forEach(category => { %>
                                                    <option value="<%= category.name %>" <%= product.category === category.name ? 'selected' : '' %>>
                                                        <%= category.name %>
                                                    </option>
                                                <% }); %>
                                            </select>
                                            
                                            <div id="category-error" class="error-message"></div>
                                        </div>
                                    </div>
                                </div>
                            </div>

                            <div class="card mb-2">
                                <div class="card-header">
                                    <h4>Edit Images</h4>
                                </div>
                                <div class="border row">
                                    <div id="addedImagesContainer" class="thumbnails-container">
                                        <% if (product.images && product.images.length > 0) { %>
                                            <% product.images.forEach((image, index) => { %>
                                                <div class="thumbnail">
                                                    <img src="/uploads/<%= image %>" alt="Image <%= index + 1 %>" style="width: 50px; height: auto;">
                                                    <span class="remove-icon" onclick="removeExistingImage('<%= image %>')">X</span>
                                                </div>
                                            <% }); %>
                                        <% } %>
                                    </div>
                                </div>

                                <div class="image-upload-section">
                                    <div class="row">
                                        <% for (let i = 1; i <= 4; i++) { %>
                                            <div class="card-body align-items-center mb-3">
                                                <img src="" alt="" id="imgView<%= i %>" class="img-upload">
                                                <input class="form-control" type="file" name="images" id="input<%= i %>" 
                                                    accept="image/png, image/jpeg, image/jpg"
                                                    onchange="viewImage<%= i %>(event), viewImage(event, <%= i %>)">
                                                <div class="image-cropper d-flex align-items-center"
                                                    style="display:none; width: 300px; height: 300px; margin-top: 10px;">
                                                    <img src="" id="croppedImg<%= i %>" alt="" class="img-cropped">
                                                    <button type="button" id="saveButton<%= i %>"
                                                        class="btn-sm btn-primary">Save</button>
                                                </div>
                                            </div>
                                        <% } %>
                                    </div>
                                </div>
                            </div>

                            <div>
                                <button class="btn btn-md rounded font-sm hover-up" type="button"
                                    onclick="validateAndSubmit()">Update Product</button>
                            </div>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </section>
</div>

<!-- Include cropper.js script and existing JavaScript functions -->
<script src="https://cdn.jsdelivr.net/npm/cropperjs@1.6.2/dist/cropper.min.js"></script>
<script>
    // JavaScript functions for image preview, cropping, validation, and form submission
    // (use the existing JavaScript functions as needed)
</script>
